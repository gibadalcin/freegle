import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  totalArea: {
    flex: 1,
  },

  sectionContainer: {
    flex: 1,
  },

  imageBackground: {
    width: '100%',
    height: '100%',
    resizeMode: 'cover',
    position: 'absolute',
  },
});

export default styles;
